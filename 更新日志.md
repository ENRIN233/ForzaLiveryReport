# 更新日志

## v1.5 - 重复检测重构为设计 GUID (2026-08-16)

### ✨ 重复检测增强

- **GUID 精确判据**：改用 `header` 末尾 16 字节设计 GUID 作为重复检测唯一判据（此前为缩略图大小 + 标题/描述文本双路径启发式）
- 同 GUID = 同设计 = 重复，精确无歧义；不同设计即使标题/作者/缩略图巧合相同也天然不会误合并
- 删除约 130 行启发式逻辑（缩略图固定锚点聚类 + 文本桶匹配 + 并查集合并）
- 经真实存档 203 涂装验证：GUID 覆盖 100%，分组结果与旧启发式完全一致（3 组重复）
- 版本变体检测不受影响，与重复检测仍互不交叉

---

## v1.4 - 版本变体检测 (2026-08-16)

### ✨ 新增功能

- **版本变体筛选**：识别同车型、同作者下仅版本号不同的涂装迭代（如 `Ma koto lim` → `Ma koto lim V3`、`...NINJIA V1` → `...NINJIA V2`），新增「仅变体」筛选按钮
- 版本号识别：`v`/`V`/`ver`/`ver.`/`version`/`final`/`FINAL`/`rev` + 数字，及小数版本（`2.0`/`3.0`）
- 与重复检测**互不交叉、独立标记**：一个涂装可同时属于重复组和变体组，两套体系各自筛选、可叠加
- 顶部统计栏显示变体组计数

### 🔧 设计说明

- 仅靠版本号剥离识别（不引入模糊匹配——经真实存档 300 涂装验证，同车型下非版本号的相似标题对为 0，模糊匹配无增量价值且增加复杂度）
- 同车型 + 同作者约束：精确区分「同人作者自己的迭代版本」（变体）与「不同玩家下载同一涂装」（重复）

---

## v1.3.2 - #号赛车品牌补全 (2026-08-13)

### 🚗 车辆品牌纠正

所有以 `#` 开头的赛车（25 辆）此前缺失车厂前缀，基于 FH6 官方数据逐码补全。覆盖 Alumicraft、Audi、Casey Currie Motorsports、Ferrari、Ford、Honda、Jimco、Mazda、Mitsubishi、Nissan、Porsche、RJ Anderson、Toyota、Volkswagen 共 14 个车厂。

---

## v1.3.1 - 年份后缀 + Formula Drift 品牌 (2026-08-13)

### 🔧 年份后缀补全（7 辆）

ModelShort 中部分年份以空格分隔（无单引号），此前未被识别，现已支持 ` XX` 格式（如 `P. 911 GT3 RS 23` → `'23`）。补全 7 辆：Charger R/T '69、Celica GT '74、Prelude Si '94、Charger SRT Hellcat '15、911 GT3 RS '23、Silvia Spec-R '02、Silvia K's '89。

同时排除误判：数字已出现在 DisplayName 中时自动跳过（`TVR Speed 12`、`G 65 AMG`、`Toyota 86`）。

### 🚗 Formula Drift 品牌纠正（13 辆）

漂移方程式车款统一为 **Formula Drift** 前缀，此前被误标为基础品牌（如 Ferrari/Toyota）或缺失。

---

## v1.3 - 双路径重复检测 (2026-08-13)

### ✨ 重复检测增强

由「缩略图主导」升级为**双路径并行 + 并集合并**：

- **路径 A（缩略图）**：原有逻辑——同车型缩略图固定锚点聚类（0.5% 容差）→ 组内文本拆分
- **路径 B（文本，新增）**：同车型 + 同作者桶内，标题必须相同；描述为空时仅标题匹配即可，双方有描述则标题和描述须双重匹配
- **合并**：并查集取并集，任意路径判定重复即算重复

---

## v1.2 - 非英语文本修复 (2026-08-12)

### 🔧 Bug 修复

- **中/日文等非英语标题、描述显示为空白**：header 解析原先只接受 ASCII 字符（高字节必须为 0），遇到中文等多字节字符即中断丢弃。已重写为按游戏的「u32 长度前缀 + UTF-16LE」格式精确解析，中文标题（如「极限竞速」「非雪」「坎特蕾拉」）与描述（如「鸣潮」「初音ミク…」）现在都能正常显示
- **顺带修复**：旧解析器会把下一个长度字段的低字节混入字符串末尾（如 `Hatsune Miku%`、`AMIYA `、`VIRTUOSA(`），以及把二进制区的 `$:4`、`08H` 等误当描述——现均已精确去除
- **默认标题恢复显示**：未起名的涂装默认标题「Forza Livery」之前会被当作哨兵清空（显示空白），现在原样保留显示；作者/描述列的哨兵清理不变

---

## v1.1 (2026-08-11)

### 🚗 车辆列表更新

同步至游戏最新版本，新增 9 辆车：

| Code | 车辆 |
|------|------|
| 1283 | Ford Thunderbird |
| 1297 | Mitsubishi Starion ESI-R |
| 1592 | Toyota Celica GT |
| 2866 | Exocet Sport V8 XP-5 |
| 3282 | #203 Porsche AG 961 |
| 3494 | Alfa Romeo Autodelta Tipo 33/2 Daytona |
| 3937 | Honda N600 |
| 3954 | Chevrolet Camaro ZL1 |
| 4238 | Nissan Skyline 2000 Turbo RS |

### ✨ 新增功能

- **「游戏内位置」列**：显示涂装在游戏中我的涂装的排列位置
- **「默认」排序按钮**：返回文件原始顺序

### 🔧 Bug 修复

- **缩略图点击放大失效**：所有缩略图无法点击放大。已修复
- **部分涂装作者显示为空**：只有作者名没有标题的涂装，作者被误放到标题栏。已修复
- **重复涂装检测失效**：缩略图大小比较过于严格，实际重复的涂装一直未被识别。改为允许微小误差
- **初始化脚本问题**：游戏更新后 `apply_all.js` 无法正常工作。已适配

---

## v1.0

- 基础涂装扫描与 HTML 报告生成
- 包含651 辆车
- 搜索过滤、排序、缩略图放大
- 文件夹一键跳转
- 重复涂装检测与高亮筛选
