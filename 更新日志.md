# 更新日志

## v1.1 (2026-08-11)

### 🚗 车辆列表更新

- **Data_Car.str 同步至最新游戏版本** 更新后的文件）
- **CAR_NAME_MAP 从 651 扩充至 660 辆车**，新增 9 辆：

| Code | 车辆 |
|------|------|
| 1283 | Ford Thunderbird |
| 1297 | Mitsubishi Starion ESI-R |
| 1592 | Toyota Celica GT |
| 2866 | Exocet Sport V8 XP-5 |
| 3282 | #203 Porsche AG 961 |
| 3494 | Alfa Romeo Autodelta Tipo 33/2 Daytona |
| 3937 | Honda N600 |
| 3954 | Chevrolet Camaro ZL1 |
| 4238 | Nissan Skyline 2000 Turbo RS |

### 🔧 Bug 修复

- **缩略图重复检测精确度过高**：Track 1（车型+缩略图文件大小）原先使用精确字节比较（`fs.statSync().size`），因 WebP 编码非确定性导致同一涂装的缩略图大小有 20-204 字节波动，命中率为 0。已改为容差聚类算法（0.5% 相对容差，先按车码分组 → 排序 → 滑动窗口聚类），现在正确识别所有真实重复组。
- **缩略图放大（Lightbox）功能修复**：`apply_all.js` 中 onclick 注入的字符串匹配模式与 `livery_analyzer.js` 实际模板不一致（变量名 `thumbSmall` vs `thumbBase64`、alt 属性结构不同），导致替换静默失败，所有 123 个缩略图均无点击放大功能。已修正匹配模式并更新守卫生条件。
- **作者/标题提取修复**：当 header 仅含 1 个字符串时，该字符串为作者名（gamertag）而非标题。先前代码将其误标为标题，导致 涂装的作者栏为空。现已针对 `strs.length === 1` 做特殊处理，正确提取作者名。
- **`apply_all.js` 三处健壮性修复**：
  1. VALUES/KEYS 区域偏移量从硬编码（`0x8C`/`0x7358`）改为从文件头动态读取（`0x84`/`0x88`），适应游戏更新后区域位置变化
  2. ModelShort 索引从硬编码 `+651` 改为动态 `+dsCount`（DisplayName 实际数量）
  3. Step 1（地图再生成）完成后未设置写盘标志 `m=true`，导致修改不会写入 `livery_analyzer.js`

### ✨ 新增功能

- **「游戏内位置」列**：在「创建日期」与「车型」之间新增一列，按文件顺序显示游戏内位置，格式为 `{N}列{M}个`（每列 2 个，如第 1 个文件为「1列1个」、第 2 个为「1列2个」、第 3 个为「2列1个」……依此类推）
- **「默认」排序按钮**：新增排序选项，点击恢复原始文件扫描顺序（即 carCode → 文件夹名的排序）。默认排序从「日期」改为「默认」，且默认按钮在页面加载时激活

### 📁 变更文件

| 文件 | 变更 |
|------|------|
| `Data_Car.str` | 从游戏安装目录提取最新版本（66485 → 67418 字节） |
| `apply_all.js` | 修复 3 处 bug + 更新 Lightbox 注入逻辑 |
| `livery_analyzer.js` | 车辆列表 651→660 + 新增游戏内位置列 + 默认排序 + 作者提取修复 |
| `启动.bat` | 已删除（旧版启动脚本） |

---

## v1.0 (初始版本)

- 基础涂装扫描与 HTML 报告生成
- 651 辆车 CAR_NAME_MAP
- 搜索过滤、日期/车型/作者排序
- 缩略图嵌入（base64）
- 文件夹链接 + 剪贴板回退
- 重复涂装检测（三轨并查集算法）
- 重复组高亮 + 「仅重复」筛选
